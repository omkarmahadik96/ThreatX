# Phishing Detection System

Advanced phishing detection system with heuristic analysis and real-time scanning.

## Structure
- `phishing-pro-max/`: Main application source code.
- `.venv/`: Python virtual environment.

## Getting Started
1. Activate the virtual environment.
2. Install dependencies (if any `requirements.txt` exists).
3. Run the application.
